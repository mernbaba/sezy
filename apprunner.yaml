version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm install
    build:
      - npm install
    post-build:
      - npx prisma generate

run:
  runtime-version: 18.19.0
  command: npm run start
  network:
    port: 3000
    env: APP_PORT
